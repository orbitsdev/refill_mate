import 'package:flutter/material.dart';

class GallonCard extends StatelessWidget {
const GallonCard({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}